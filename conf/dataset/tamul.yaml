# @package _group_

name: tamul_lightfield
collection: cars_1
data_subdir: tamul
root_dir: "{config.experiment.data_dir}/{config.dataset.data_subdir}/{config.dataset.collection}/"

img_wh: [540, 372]
spherical_poses: False
use_ndc: False

val_num: 200
val_skip: 1
val_all: False

render_params:
  interpolate: False
  supersample: 4
  crop: 1.0

lightfield:
  rows: 8
  cols: 8
  step: 7
  supersample: 2
  disp_row: 3

  st_scale: 0.05
  vis_st_scale:
